language: cpp

matrix:
  include:
    - os: osx
      osx_image: xcode11.6

dist: trusty

before_install:
  - chmod +x ./.travis/install.sh
  -
install:
  - ./.travis/install.sh

before_script:
  - g++ --version
  - PARENTDIR=$(pwd)
  - mkdir $PARENTDIR/build && cd $PARENTDIR/build

script:
  - make
